@model IEnumerable<practica2Web.Models.Usuario>
@{
    ViewData["Title"] = "Usuarios";
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h4 class="card-title mb-0">
                    <i class="mdi mdi-account-multiple mr-2 text-info"></i> Lista de Usuarios
                </h4>
                <a asp-action="Create" class="btn btn-info btn-rounded waves-effect waves-light">
                    <i class="mdi mdi-account-plus"></i> Nuevo Usuario
                </a>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th style="width:50px;">#</th>
                                <th>Nombre</th>
                                <th>Email</th>
                                <th>Dirección</th>
                                <th>Teléfono</th>
                                <th style="width:110px; text-align:center;">Nacimiento</th>
                                <th class="text-center" style="width:120px;">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="text-primary font-weight-bold">@item.Id</td>
                                    <td><strong>@item.Nombre</strong></td>
                                    <td>@item.Email</td>
                                    <td>@item.Perfil?.Direccion</td>
                                    <td>@item.Perfil?.Telefono</td>
                                    <td class="text-center">@item.Perfil?.FechaNacimiento.ToShortDateString()</td>
                                    <td class="text-center">
                                        <a asp-action="Edit" asp-route-id="@item.Id"
                                           class="btn btn-warning btn-sm btn-rounded waves-effect" title="Editar">
                                            <i class="mdi mdi-pencil"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id"
                                           class="btn btn-danger btn-sm btn-rounded waves-effect" title="Eliminar">
                                            <i class="mdi mdi-delete"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                            @if (!Model.Any())
                            {
                                <tr>
                                    <td colspan="7" class="text-center py-4 text-muted">
                                        <i class="mdi mdi-account-off" style="font-size:32px; display:block; margin-bottom:8px;"></i>
                                        No hay usuarios registrados
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
